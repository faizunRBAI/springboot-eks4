apiVersion: v1
kind: ServiceAccount
metadata:
  name: api
  namespace: '%%NAMESPACE%%'
  labels:
    app.kubernetes.io/name: api
# A dedicated identity with no role bindings: the application needs no
# Kubernetes API access, so it gets none.
automountServiceAccountToken: false
